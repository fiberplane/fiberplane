# prometheus-provider-ts

> Native TypeScript implementation of the Prometheus Provider.

Our Prometheus Provider is used in several TypeScript codebases where it would
be overkill to use a full Provider runtime just to use a single provider.

Please see https://github.com/fiberplane/providers for the original provider
written in Rust.
