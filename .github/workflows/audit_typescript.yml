name: TypeScript package audit

on:
  push:
    paths:
      - ".github/workflows/audit_typescript.yml"
      - "**/package.json"
  schedule:
    - cron: "0 0 * * *"

jobs:
  security_audit_ts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: yarn

      - name: Run security audit
        run: yarn workspaces foreach npm audit
